# PromptForge実装のためのAIエージェント指示書
**（個人プロトタイプ版）**

## 1. 基本方針

### 1.1 指示の基本原則
- **「何を」だけでなく「どのように」を明確に伝える**
- **どこから始めるかを具体的に指示する**
- **防御的プロンプティングを実践する**
- **段階的に実装し、各段階でチェックポイントを設ける**

### 1.2 プロトタイプ版の特徴
- **個人使用前提**：セキュリティ要件は最小限
- **速度よりも動作**：完璧なパフォーマンスよりも機能実装を優先
- **品質は重視**：自動テストで品質を確保
- **技術的挑戦歓迎**：IndexedDBやasearchなど適切な技術を使用

### 1.3 実装の全体フロー
```
Phase 1: 基本動作 → Phase 2: 核心機能 → Phase 3: 便利機能
     ↓              ↓              ↓
  自動テスト     →  自動テスト   →  自動テスト
```

## 2. Phase 1: 基本動作（まず動くものを作る）

### 2.1 プロジェクト初期化指示

```markdown
【指示例】
プロトタイプ版「PromptForge」のReact + TypeScriptアプリケーションを作成してください。

技術スタック：
- React 18 + TypeScript
- Vite（ビルドツール）
- IndexedDB（データストレージ）
- Tailwind CSS（スタイリング）
- asearch（検索ライブラリ）

テスト環境：
- Jest + React Testing Library（単体テスト）
- Playwright（E2Eテスト）
- Testing Library Jest DOM（DOM テスト）

フォルダ構成：
```
src/
├── components/     # UIコンポーネント
├── hooks/         # カスタムフック
├── services/      # データアクセス・API
├── types/         # TypeScript型定義
├── utils/         # ユーティリティ関数
├── stores/        # 状態管理
└── __tests__/     # テストファイル
```

最初に以下を実装してください：
1. プロジェクトの初期化（package.json, tsconfig.json等）
2. 基本的なコンポーネント構造
3. 型定義ファイル（types/index.ts）
4. IndexedDBのセットアップ
5. テスト環境の設定

注意事項：
- 各ファイルには適切なTypeScript型を使用する
- ESLintとPrettierの設定も含める
- テストファイルは必ず同時に作成する
- 実装前に全体的なファイル構成を確認してください
```

### 2.2 データモデル実装指示

```markdown
【指示例】
KnowledgeItemの型定義とIndexedDBスキーマを実装してください。

参考資料：詳細設計書のKnowledgeItemインターフェース

実装要件：
1. 完全なTypeScript型定義（types/index.ts）
2. IndexedDB初期化スクリプト（services/database.ts）
3. 基本的なCRUD操作（services/knowledgeService.ts）

必須機能：
- 知識アイテムの作成・読み取り・更新・削除
- IDの自動生成
- 作成日時・更新日時の自動設定
- データの検証機能

テスト要件（必須）：
- 各CRUD操作の単体テスト
- エラーハンドリングのテスト
- データ整合性の確認
- IndexedDBの初期化テスト

チェックポイント：
実装完了後、以下を確認してください：
1. TypeScriptエラーがないか
2. すべての単体テストが通るか
3. テストカバレッジが80%以上か
4. ブラウザでIndexedDBが正しく作成されるか
```

### 2.3 基本UI実装指示

```markdown
【指示例】
メイン画面の基本レイアウトとコンポーネントを実装してください。

デザイン要件：
- デスクトップ中心（モバイル対応は後回し）
- 基本的なアクセシビリティ対応
- 機能概要.mdのUI mockupを参考

実装コンポーネント：
1. SearchBar（検索バー）
2. KnowledgeCard（知識カード）
3. KnowledgeList（知識一覧）
4. AddKnowledgeForm（新規追加フォーム）
5. Layout（全体レイアウト）

各コンポーネントの要件：
- SearchBar: 基本的な検索入力
- KnowledgeCard: タイトル、タグ、使用回数の表示
- KnowledgeList: カード一覧の表示
- AddKnowledgeForm: 知識入力フォーム
- Layout: ヘッダー、メインコンテンツ、フッター

テスト要件（必須）：
- 各コンポーネントの単体テスト
- プロパティの型チェック
- レンダリングテスト
- イベントハンドリングのテスト

スタイリング：
- Tailwind CSSで基本的なスタイル
- 見た目の完璧さより機能を優先

注意事項：
- 各コンポーネントはpropsの型定義を含める
- テストファイルは必ず同時に作成する
- テストカバレッジ80%以上を目指す
```

## 3. Phase 2: 核心機能実装

### 3.1 曖昧検索エンジン実装指示

```markdown
【指示例】
asearchライブラリを使用した曖昧検索エンジンを実装してください。

参考資料：詳細設計書のFuzzySearchEngineクラス

実装要件：
1. 複数フィールドでの検索（タイトル、タグ、説明文）
2. リアルタイム検索（入力中の逐次絞り込み）
3. 検索結果のスコアリング
4. 検索履歴の保存

パフォーマンス要件：
- 個人使用レベルで十分（100件程度の知識で快適）
- 検索速度は1秒以内で動作すれば十分

テスト要件（必須）：
- 各種検索パターンの単体テスト
- パフォーマンステスト
- エラーケースのテスト
- 検索結果のスコアリングテスト
- 検索履歴の保存・読み込みテスト

E2Eテスト要件（必須）：
- 検索フローの完全なテスト
- 検索結果の表示確認
- 検索履歴の動作確認

チェックポイント：
1. 基本的な検索機能が動作するか
2. タイプミスに対する許容度が適切か
3. すべてのテストが通るか
4. E2Eテストで実際の使用感を確認
```

### 3.2 知識エディター実装指示

```markdown
【指示例】
知識入力・編集フォームを実装してください。

UI要件：
- 基本的な入力フォーム
- 保存・キャンセル機能
- リアルタイム検証

実装コンポーネント：
1. KnowledgeForm（統合入力フォーム）
2. SaveButton（保存ボタン）
3. CancelButton（キャンセルボタン）
4. ValidationMessage（検証メッセージ）

機能要件：
- リアルタイム入力検証
- 保存確認メッセージ
- エラーメッセージ表示
- タグの自動補完

テスト要件（必須）：
- フォーム入力の単体テスト
- バリデーションのテスト
- 保存・キャンセル機能のテスト
- エラーハンドリングのテスト

E2Eテスト要件（必須）：
- 知識作成フローの完全なテスト
- 知識編集フローの完全なテスト
- バリデーションエラーの表示確認
- 保存後のデータ確認

バリデーション：
- タイトルの必須チェック
- データ形式の検証
- 重複チェック

注意事項：
- 各機能に対応するテストを必ず作成
- テストファーストアプローチを推奨
- テストカバレッジ90%以上を目指す
```

## 4. Phase 3: 便利機能実装

### 4.1 AI連携機能実装指示（簡易版）

```markdown
【指示例】
基本的なAI連携機能を実装してください。

セキュリティ要件：
- 環境変数でのAPIキー管理（個人使用レベル）
- 基本的なエラーハンドリング

実装機能：
1. 基本的なプロンプト提案
2. 簡単な知識整理支援

テスト要件（必須）：
- API呼び出しのモックテスト
- エラーハンドリングのテスト
- 非同期処理のテスト
- レスポンスデータの処理テスト

E2Eテスト要件（必須）：
- AI機能の完全なフローテスト
- エラーケースの表示確認
- API制限時の動作確認

エラーハンドリング：
- API接続エラーの基本的な処理
- わかりやすいエラーメッセージ
- レート制限の適切な処理

注意事項：
- モックを使った単体テストを重視
- 実際のAPI呼び出しはE2Eテストで確認
- テストカバレッジ85%以上を目指す
```

### 4.2 データ可視化機能実装指示（簡易版）

```markdown
【指示例】
シンプルな統計表示を実装してください。

可視化要件：
1. 基本的な使用回数表示
2. 知識の総数表示
3. 最近使用した知識の表示

使用ライブラリ：
- Chart.jsで基本的なグラフ
- 複雑な可視化は後回し

テスト要件（必須）：
- グラフ描画の単体テスト
- データ変換処理のテスト
- 統計計算のテスト
- レスポンシブ対応のテスト

E2Eテスト要件（必須）：
- 統計画面の表示確認
- グラフの正しい描画確認
- データ更新時の再描画確認

実装順序：
1. 基本的な数値表示
2. 簡単なグラフ表示
3. 見た目の調整

チェックポイント：
1. 基本的な統計が表示されるか
2. すべてのテストが通るか
3. E2Eテストで実際の動作確認
```

## 5. 品質保証・テスト指示（自動テスト重視）

### 5.1 テスト実装指示

```markdown
【指示例】
包括的なテストスイートを実装してください。

テスト種別（すべて必須）：
1. 単体テスト（Jest + React Testing Library）
2. 統合テスト（データフロー全体）
3. E2Eテスト（Playwright）

テストカバレッジ要件：
- 最低85%のカバレッジ
- 重要な機能は95%以上のカバレッジ
- すべてのエラーケースをカバー

CI/CD統合：
- GitHub Actionsワークフロー
- 自動テスト実行
- カバレッジレポート
- テスト失敗時のPRブロック

パフォーマンステスト：
- 検索機能の応答時間
- 大量データでの動作
- メモリ使用量の監視

E2E テスト要件：
- 主要な機能フローのテスト
- ユーザージャーニーの確認
- エラーケースの動作確認
- ブラウザ互換性の確認

注意事項：
- テストファーストアプローチを推奨
- 各機能実装時にテストも同時作成
- テストの保守性も重視
```

## 6. 運用・保守指示（簡易版）

### 6.1 基本的なログ・エラー処理

```markdown
【指示例】
基本的なエラーハンドリングを実装してください。

ログ出力要件：
- コンソールへの構造化ログ出力
- エラー発生時の詳細情報記録
- パフォーマンス情報の記録

監視項目：
- アプリケーションの可用性
- 検索機能の応答時間
- エラー発生率

テスト要件（必須）：
- ログ出力の単体テスト
- エラーハンドリングのテスト
- パフォーマンス監視のテスト

実装要件：
- わかりやすいエラーメッセージ
- ユーザーフレンドリーな表示
- 開発者向けデバッグ情報

注意事項：
- 個人使用レベルで十分
- ログもテスト対象に含める
```

## 7. プロトタイプ版の注意事項

### 7.1 優先度の考え方
- **最優先**: 基本的な機能が動作すること
- **同等に重要**: 自動テストで品質を保証すること
- **二番目**: 使いやすさ・安定性
- **三番目**: 見た目・高度な機能

### 7.2 妥協できる部分
- **セキュリティ**: 個人使用レベルで十分
- **パフォーマンス**: 快適に動作すれば十分
- **見た目**: 機能性を優先

### 7.3 重要な部分
- **データの保存・読み込み**: 確実に動作する
- **基本的な検索**: 知識を見つけられる
- **知識の追加・編集**: 情報を蓄積できる
- **自動テスト**: 品質を保証する

### 7.4 テスト戦略
- **単体テスト**: 各機能の動作保証
- **E2Eテスト**: ユーザー体験の保証
- **継続的テスト**: CI/CDでの自動実行
- **テストカバレッジ**: 85%以上を維持

## 8. 成功のための追加ポイント

### 8.1 テストファーストアプローチ
- **テスト設計**: 機能実装前にテストを設計
- **TDD実践**: テスト→実装→リファクタリング
- **品質保証**: 自動テストで継続的な品質確保

### 8.2 実用性重視
- **実際に使う**: 自分で使いながら改善
- **フィードバック**: 使いにくい部分を改善
- **継続的改善**: 少しずつ品質向上

### 8.3 現実的な目標
- **技術的挑戦**: 適切な技術スタックを使用
- **品質重視**: テストで品質を保証
- **段階的発展**: 使いながら改善していく
